# Build artifacts — the build stage compiles from source
_build/
deps/

# Zig build artifacts — only the solver binary is needed
native/zig-cache/
native/zig-out/
native/HiGHS/
native/lp_data/
native/util/
native/build/
native/*.o
native/*.zig
native/solver.zig:Zone.Identifier
# NOTE: native/solver is intentionally NOT excluded — it's copied into the image

# Dev/test
cover/
doc/
tmp/
test/
.elixir_ls/
.formatter.exs

# Secrets — never send to Docker daemon
.env
.env.local

# Git
.git/
.gitignore

# Editor/OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Logs
*.log
erl_crash.dump

# Node
node_modules/

# Fly / Docker
fly.toml
Dockerfile
.dockerignore
